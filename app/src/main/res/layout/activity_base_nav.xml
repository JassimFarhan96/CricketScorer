<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Content + floating bug-report button overlaid together -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Child activity content -->
        <FrameLayout
            android:id="@+id/nav_content_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- Floating bug-report button — top-right corner, always on top -->
        <ImageButton
            android:id="@+id/btn_report_bug"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="44dp"
            android:layout_marginEnd="14dp"
            android:background="@drawable/bg_bug_report_btn"
            android:src="@drawable/ic_home_bug"
            android:scaleType="centerInside"
            android:padding="6dp"
            android:elevation="6dp"
            android:contentDescription="Report a bug"
            android:visibility="gone"/>

    </FrameLayout>

    <!-- Bottom navigation bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/c_bg_card"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
